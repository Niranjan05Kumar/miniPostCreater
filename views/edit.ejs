<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white sm:p-10 p-[5vw]">
        <h1 class="w-fit mb-6 text-3xl font-semibold">Edit your port</h1>
        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content"
                class="inline-block outline-none bg-zinc-800 border-[2px] border-zinc-700 px-4 py-2 mt-3 resize-none rounded-md w-full sm:w-1/3"> <%= post.content %></textarea>
            <input type="submit"
                class="block w-fit py-1 px-4 bg-yellow-500 hover:bg-yellow-400 rounded-md mt-1 transition"
                value="Update post">
        </form>
    </div>
</body>

</html>